x-crypto:
    &crypto
    cap_drop:
      - all
    cap_add:
      - chown
      - setuid
      - setgid
      - sys_admin
      - mknod
    security_opt:
      - apparmor=unconfined
      - seccomp=unconfined

services:
    length-extension:
        << : *crypto
        build : .
        ports :
            - "61300:5000"
